_build
.build
bin/Version.ml
SiNPle.opam
.claude
# Release archives and payload dirs are build artifacts; the release
# source-of-truth (MANIFEST, CURRENT) stays tracked.
releases/CURRENT~
releases/SiNPle-*
